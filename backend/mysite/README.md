# 📔 Guia de configuração de ambiente

Rodar nosso projeto na sua máquina nunca foi tão simples. Neste caso tudo que você precisa é ter o [Python <= 3.11](https://www.python.org/downloads/) instalado.

## 🖥 Windows

Apenas execute o seguinte arquivo no seu powershell -> `.\rodar_backend.bat`

## 🐧 Linux


Caso não tenha instalado o `python3.12-venv` e o `python3-pip` na sua distribuição linux, execute o comando `sudo apt install python3.12-venv python3-pip`

Altere a permissão do arquivo para poder executá-lo com `sudo chmod u+x ./construir_backend.sh` e `sudo chmod u+x ./rodar_backend.sh`


Após os passos anteriores, apenas execute o seguinte arquivo na sua bash -> `./rodar_backend.sh`
